import { JSX } from "preact";
import { UploadWidgetConfigRequired } from "../../../../../config/UploadWidgetConfig";
interface Props {
    imageCount: number;
    imageIndex: number;
    options: UploadWidgetConfigRequired;
}
export declare const getImageEditorHeader: ({ imageCount, imageIndex, options }: Props) => JSX.Element | undefined;
export {};
