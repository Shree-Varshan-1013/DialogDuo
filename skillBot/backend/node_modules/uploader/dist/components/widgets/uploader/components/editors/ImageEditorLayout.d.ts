import { JSX } from "preact";
import { ReactNode } from "../../../../../modules/common/React";
import { RectWithPos } from "../../../../../modules/common/Rect";
import { UploadedFile } from "upload-js";
import "./ImageEditorLayout.scss";
interface Props {
    actions: ReactNode;
    header?: ReactNode;
    image?: (props: {
        imageUrl: string;
        imgDimensions: RectWithPos;
    }) => ReactNode;
    modal: boolean;
    originalImage: UploadedFile;
}
export declare const ImageEditorLayout: ({ actions, originalImage, header, image, modal }: Props) => JSX.Element;
export {};
