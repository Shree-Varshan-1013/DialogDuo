import { JSX } from "preact";
import "./ProgressIcon.scss";
interface Props {
    height: number;
    isError: boolean;
    onCompleteImageSource: string;
    progress: number;
}
export declare const progressWheelVanish = 300;
export declare const progressWheelDelay = 350;
export declare const ProgressIcon: ({ height, progress, onCompleteImageSource, isError }: Props) => JSX.Element;
export {};
