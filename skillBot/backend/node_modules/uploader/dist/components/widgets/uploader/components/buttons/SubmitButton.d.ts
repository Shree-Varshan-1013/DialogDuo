import { JSX } from "preact";
import { UploadWidgetLocale } from "../../../../..";
interface Props {
    busyText: string;
    idleText: string;
    locale: UploadWidgetLocale;
    onSubmit: () => Promise<void>;
    showIcon: boolean;
}
export declare const SubmitButton: ({ busyText, idleText, locale, onSubmit, showIcon }: Props) => JSX.Element;
export {};
