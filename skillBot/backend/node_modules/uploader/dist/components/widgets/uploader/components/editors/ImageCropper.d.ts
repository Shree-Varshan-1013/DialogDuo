import { JSX } from "preact";
import { UploadedFile, UploadInterface } from "upload-js";
import { UploadWidgetConfigRequired } from "../../../../../config/UploadWidgetConfig";
import "./ImageCropper.scss";
interface Props {
    imageCount: number;
    imageIndex: number;
    onFinish: (keep: boolean, editedFile: UploadedFile | undefined) => void;
    options: UploadWidgetConfigRequired;
    originalImage: UploadedFile;
    upload: UploadInterface;
}
export declare const ImageCropper: (props: Props) => JSX.Element;
export {};
