import { JSX } from "preact";
import { UploadWidgetConfigRequired } from "../../../../../config/UploadWidgetConfig";
interface Props {
    onFinish: (keep: boolean) => Promise<void>;
    options: UploadWidgetConfigRequired;
}
export declare const ImageEditorButtons: ({ options, onFinish }: Props) => JSX.Element;
export {};
