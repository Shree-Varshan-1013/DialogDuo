import { JSX } from "preact";
import { Rect, RectWithPos } from "../../../../../../modules/common/Rect";
import "./ResizableSquare.scss";
interface Props {
    boundary: Rect;
    children: JSX.Element;
    onResized: (geometry: {
        boundary: Rect;
        geometry: RectWithPos;
    } | undefined) => void;
    ratio: number | undefined;
}
export declare const ResizableSquare: ({ boundary, ratio, onResized, children }: Props) => JSX.Element;
export {};
