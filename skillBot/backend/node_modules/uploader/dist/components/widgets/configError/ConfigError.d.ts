import { JSX } from "preact";
import { UploadWidgetLayout } from "../../../config/UploadWidgetLayout";
interface Props {
    error: Error;
    layout: UploadWidgetLayout;
}
export declare const ConfigError: ({ error, layout }: Props) => JSX.Element;
export {};
