import { ReactNode } from "../../../modules/common/React";
import { JSX } from "preact";
import "./WidgetBase.scss";
import { UploadWidgetLayout } from "../../../config/UploadWidgetLayout";
interface Props {
    children: ReactNode;
    htmlProps?: any;
    isDraggable?: boolean;
    isDragging?: boolean;
    layout: UploadWidgetLayout;
    multi: boolean;
}
export declare const WidgetBase: ({ children, htmlProps, isDraggable, isDragging, layout }: Props) => JSX.Element;
export {};
