import { JSX } from "preact";
import { Rect } from "../../../modules/common/Rect";
interface Props {
    closeButtonSize: number;
    dimensions: Rect | undefined;
    isDragging: boolean;
}
export declare const WidgetBaseBackground: ({ closeButtonSize, isDragging, dimensions }: Props) => JSX.Element;
export {};
