import { JSX } from "preact";
export interface Rect {
    height: number;
    width: number;
}
export interface RectWithPos extends Rect {
    x: number;
    y: number;
}
export declare namespace RectWithPos {
    function toCssProps(r: RectWithPos): Partial<JSX.CSSProperties>;
}
