export declare class DataTaggedElementTracker {
    private readonly dataAttributes;
    private readonly onFind;
    private readonly seenElements;
    private readonly bindInlineWidgetsDelay;
    constructor(dataAttributes: string[], onFind: (element: HTMLElement) => void);
    findElementsAndMonitor(): void;
    private findElements;
    private addDomMonitor;
    private raiseUnseenElement;
}
