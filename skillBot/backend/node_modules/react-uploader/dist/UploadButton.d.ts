/// <reference types="react" />
import { UploaderInterface, UploadWidgetResult, UploadWidgetConfig } from "uploader";
interface MouseEventLite {
    preventDefault: () => void;
}
interface Props {
    children: (props: {
        onClick: (event: MouseEventLite) => void;
    }) => JSX.Element;
    onComplete?: (files: UploadWidgetResult[]) => void;
    options?: UploadWidgetConfig;
    uploader: UploaderInterface;
}
export declare const UploadButton: ({ uploader, options, onComplete, children }: Props) => JSX.Element;
export {};
