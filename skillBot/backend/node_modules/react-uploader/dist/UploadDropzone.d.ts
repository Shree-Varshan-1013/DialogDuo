import { UploaderInterface, UploadWidgetResult, UploadWidgetConfig } from "uploader";
interface Props {
    className?: string;
    height?: string;
    minWidth?: string;
    onComplete?: (files: UploadWidgetResult[]) => void;
    onUpdate?: (files: UploadWidgetResult[]) => void;
    options?: UploadWidgetConfig;
    uploader: UploaderInterface;
    width?: string;
}
export declare const UploadDropzone: ({ uploader, options, onComplete, onUpdate, minWidth, width, height, className }: Props) => JSX.Element;
export {};
